# Build output (binary name fixed for `gh extension install`)
/gh-orbit

# GoReleaser snapshot / release artifacts
/dist/

# Go workspace / vendor
/vendor/
*.test
*.out
coverage.txt

# Editor / OS
.DS_Store
.idea/
.vscode/

# Claude Code per-user settings (project-shared SKILL.md is tracked)
.claude/settings.local.json

# AI coding agent local artifacts
.context/
.antigravitycli/
