# Demo scratch (throwaway)

This note backs a throwaway PR used only while recording the
gh-orbit hero GIF — it exists so the worktree card shows a live
`#N` + CI badge and the `O` review overlay has a real diff to open.

It will be closed and deleted right after the recording.
